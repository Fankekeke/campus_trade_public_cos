<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.mrbird.febs.cos.dao.DrugInfoMapper">

    <!-- 分页获取商品信息 -->
    <select id="selectDrugPage" resultType="java.util.LinkedHashMap">
        select
        id,
        code,
        name,
        brand,
        category,
        classification,
        common_name,
        dosage_form,
        usages,
        applicable_symptoms,
        applicable_disease,
        packing_list,
        dosage_use,
        validity_period,
        approval_number,
        manufacturer,
        unit_price,
        create_date,
        images
        from
        drug_info
        where
        1 = 1
        <if test="goodsInfo.code != null and goodsInfo.code != ''">
            AND code LIKE CONCAT('%',#{goodsInfo.code},'%')
        </if>
        <if test="goodsInfo.name != null and goodsInfo.name != ''">
            AND name LIKE CONCAT('%',#{goodsInfo.name},'%')
        </if>
        <if test="goodsInfo.brand != null and goodsInfo.brand != ''">
            AND brand LIKE CONCAT('%',#{goodsInfo.brand},'%')
        </if>
        <if test="goodsInfo.category != null">
            AND category = #{goodsInfo.category}
        </if>
        order by
        create_date desc
    </select>
</mapper>
